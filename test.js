'use strict';

const toString = require('./index');

describe("test make string", () => {
    it("test string with single quote", () => {

    });

    it("test string with double quote", () => {

    });
});